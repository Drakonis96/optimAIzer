# ====================================
# optimAIzer - Environment Variables
# ====================================
# Copy this file to .env and fill in your API keys.
# IMPORTANT: The .env file should NEVER be committed to version control.
#
# API keys are stored and used ONLY on the server side.
# They are never exposed to the client/browser.
# ====================================

# --- AI Provider API Keys ---
# Get your keys from the provider dashboards:
# OpenAI:      https://platform.openai.com/api-keys
# Anthropic:   https://console.anthropic.com/settings/keys
# Google:      https://aistudio.google.com/apikey
# Groq:        https://console.groq.com/keys
# OpenRouter:  https://openrouter.ai/keys

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
GROQ_API_KEY=
OPENROUTER_API_KEY=

# --- Local Providers (optional) ---
# Ollama default API endpoint:
# OLLAMA_BASE_URL=http://127.0.0.1:11434
#
# LM Studio OpenAI-compatible endpoint:
# LMSTUDIO_BASE_URL=http://127.0.0.1:1234

OLLAMA_BASE_URL=http://127.0.0.1:11434
LMSTUDIO_BASE_URL=http://127.0.0.1:1234

# --- Server Configuration ---
PORT=3001
CORS_ORIGIN=http://localhost:3000
NODE_ENV=development

# --- Streaming Cache (optional) ---
# Response cache for identical prompts across providers (saves repeated token spend).
# STREAM_CACHE_ENABLED=true
# STREAM_CACHE_TTL_MS=600000
# STREAM_CACHE_MAX_ENTRIES=500

# --- Agent Credentials Encryption (recommended) ---
# Secret used to encrypt agent web credentials at rest.
# If empty, the server auto-generates a local key in data/.agent-credentials.key.
AGENT_CREDENTIALS_ENCRYPTION_KEY=

# --- Host Filesystem Access (Docker only) ---
# When running in Docker, mount host directories so the agent can access
# your files (Desktop, Documents, etc.).  Configured in docker-compose.yml.
#
# macOS:   HOST_HOME is /Users          (default)
# Linux:   HOST_HOME=/home
# OMV:     HOST_HOME=/home  (+ extra mounts in docker-compose.yml)
# Windows: HOST_HOME=/mnt/c/Users  (Docker Desktop + WSL2)
#
# Override if your home base differs from the default:
# HOST_HOME=/home
